module.exports = {
    name: 'guildCreate',
    execute(guild, client) {
      console.log(`Joined a new guild: ${guild.name}`);
      // Additional logic when the bot joins a new guild
    },
  };
  